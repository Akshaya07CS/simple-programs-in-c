#include <stdio.h>
int main() {
    int A, B, temp;
    printf("Enter A");
    scanf("%d", &A);
    printf("Enter B");
    scanf("%d", &B);
    printf("Before swapping:%d %d", A, B);
    temp = A;
    A = B;
    B = temp;
    printf(" After swapping:%d %d", A, B);
    return 0;
}
